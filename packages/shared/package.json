{
  "name": "@fullstack-monorepo-demo/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for fullstack monorepo",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --emitDeclarationOnly",
    "dev": "tsc --watch --emitDeclarationOnly",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["shared", "types", "prisma"],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.14.0",
  "devDependencies": {
    "typescript": "^5.1.3"
  },
  "peerDependencies": {
    "@prisma/client": "*"
  }
}
